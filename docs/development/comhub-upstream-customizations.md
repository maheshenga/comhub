# ComHub Upstream Customizations

This document records ComHub-specific changes that must be preserved when syncing from upstream LobeHub.

Rule for future changes: every ComHub customization that touches upstream-owned files must add a short entry here before commit. Keep the entry specific enough that a future upstream merge can re-apply or verify the customization quickly.

## 2026-05-25 Auth, Brand, Provider, Community, Onboarding

| Area | Files | Change | Why | Upstream Sync Note | Tests |
| --- | --- | --- | --- | --- | --- |
| Auth runtime brand | `src/app/[variants]/(auth)/layout.tsx`, `src/app/[variants]/(auth)/_layout/index.tsx` | Inject `BrandProvider` into auth routes, generate auth metadata from the runtime brand, and use runtime brand logo/name/copyright in auth chrome. | Auth pages are outside the SPA shell, so they did not receive backend brand settings. | Preserve the auth `ClientOnly`/`NuqsAdapter` hydration shape; keep `ClientOnly` as the direct auth client boundary and place `BrandProvider initialBrand={getServerBrand()}` inside it around the auth container. | Brand/auth covered through brand provider and metadata tests. |
| Auth copy | `src/app/[variants]/(auth)/signin/SignInEmailStep.tsx`, `src/app/[variants]/(auth)/signin/SignInPasswordStep.tsx`, `src/app/[variants]/(auth)/signup/[[...signup]]/BetterAuthSignUpForm.tsx`, `src/app/[variants]/(auth)/signup/[[...signup]]/page.tsx` | Use backend `brand.authTitle` and runtime brand name for signin/signup copy and signup metadata. | Login/register pages must show the admin-configured brand, not LobeHub defaults. | Do not restore hardcoded `BRANDING_NAME` or `Agent teammates that grow with you` in auth form titles. | `src/server/metadata.test.ts`. |
| Runtime brand schema | `src/features/Brand/BrandProvider.tsx`, `src/server/services/brand/index.ts`, `src/server/services/appSettings/index.ts`, `src/business/server/lambda-routers/admin/settings.ts`, `src/features/Admin/adminSettingsForm.ts`, `src/features/Admin/AdminSettingsPage.tsx` | Add backend-configurable `homeMessengerEnabled`, `homeMessengerBannerTitle`, and `communityForkAndChatLabel` public brand fields. | Home chat-platform entry and community fork button text need admin control. | Keep these keys public and brand-cache refreshing: `home.messenger.enabled`, `home.messengerBanner.title`, `community.forkAndChat.label`. | `src/features/Admin/adminSettingsForm.test.ts`, `src/server/services/brand/__tests__/index.test.ts`, `src/features/Brand/BrandProvider.test.tsx`. |
| SPA page title | `src/server/metadata.ts`, `src/app/spa/[variants]/[[...path]]/route.ts` | Allow metadata site name override and use runtime brand for SPA `og:site_name`. | Page titles and SEO metadata should not leak LobeHub branding after admin branding is set. | Keep `siteName` optional so upstream metadata callers remain compatible. | `src/server/metadata.test.ts`, `src/app/spa/[variants]/[[...path]]/route.test.ts`. |
| Home chat platform banner | `src/routes/(main)/home/features/InputArea/index.tsx`, `src/routes/(main)/home/features/InputArea/bannerCandidates.ts`, `src/routes/(main)/home/features/InputArea/MessengerBanner.tsx` | Use backend `homeMessengerEnabled` to control whether the entry appears, use `homeMessengerBannerTitle` for copy, and replace legacy Lobe AI/LobeHub tokens with runtime brand. | The “聊天平台” homepage entry must be configurable and branded. | Preserve `MESSENGER_BANNER_ID` unless the visible UX materially changes; do not re-add the messenger banner candidate without checking `brand.homeMessengerEnabled`. | `src/routes/(main)/home/features/InputArea/bannerCandidates.test.ts`, plus brand settings tests. |
| Community fork flow | `src/layout/AuthProvider/MarketAuth/types.ts`, `src/layout/AuthProvider/MarketAuth/MarketAuthProvider.tsx`, `src/routes/(main)/community/(detail)/agent/features/Sidebar/ActionButton/ForkAndChat.tsx`, `src/routes/(main)/community/(detail)/group_agent/features/Sidebar/ActionButton/ForkGroupAndChat.tsx` | Add `signIn({ skipProfileSetup: true })` and backend-configurable fork button label. | “派生并聊天” should authenticate and fork directly instead of blocking on community profile creation. | Keep ordinary profile/user entry points on the upstream profile setup flow; only fork buttons skip it. | Manual/community flow smoke recommended after build. |
| System agent model select | `src/routes/(main)/settings/agent/features/SystemAgentForm.tsx`, `src/features/ModelSelect/index.tsx` | Bind `ModelSelect` value and avoid passing `undefined/undefined` as selected value. | The “新建助理” system-agent model picker could not switch reliably. | Preserve controlled value binding when upstream updates `ModelSelect`. | Existing model-select usages should be covered by targeted UI smoke. |
| Admin-managed providers | `src/server/globalConfig/index.ts`, `packages/database/src/repositories/aiInfra/index.ts`, `src/routes/(main)/settings/provider/features/ProviderConfig/index.tsx`, `src/store/aiInfra/slices/aiProvider/action.ts` | Business builds no longer force DeepSeek or anonymous built-in providers, and user provider toggles cannot resurrect providers outside backend config. | New users should only see/use admin-configured AI services. | In business mode, backend provider config is authoritative; do not re-enable user frontend provider switches. | `src/server/globalConfig/index.business.test.ts`, `packages/database/src/repositories/aiInfra/__tests__/getAiProviderList.test.ts`. |
| Onboarding fallback agents | `src/const/onboardingAgentTemplates.ts`, `src/routes/onboarding/features/AgentPickerStep/index.tsx`, `src/services/installMarketplaceAgents.ts` | Add local fallback Agent templates and local install path when marketplace templates are empty/unavailable. | Signup onboarding should still show Agents and create assistants when upstream marketplace is unavailable. | Keep local IDs prefixed with `comhub-local-onboarding-` to avoid collision with marketplace IDs. | `src/routes/onboarding/features/AgentPickerStep/index.test.tsx`, `src/services/installMarketplaceAgents.test.ts`. |
