import { memo } from 'react';

const ArtifactsTable = memo(() => {
  return <div data-testid="admin-module-app-artifacts" />;
});

ArtifactsTable.displayName = 'ArtifactsTable';

export default ArtifactsTable;
