import { memo } from 'react';

const ActionEditor = memo(() => {
  return <div data-testid="admin-module-app-action-editor" />;
});

ActionEditor.displayName = 'ActionEditor';

export default ActionEditor;
