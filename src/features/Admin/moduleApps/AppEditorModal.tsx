import { memo } from 'react';

const AppEditorModal = memo(() => {
  return <div data-testid="admin-module-app-editor" />;
});

AppEditorModal.displayName = 'AppEditorModal';

export default AppEditorModal;
