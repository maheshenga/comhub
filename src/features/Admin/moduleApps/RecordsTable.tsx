import { memo } from 'react';

const RecordsTable = memo(() => {
  return <div data-testid="admin-module-app-records" />;
});

RecordsTable.displayName = 'RecordsTable';

export default RecordsTable;
