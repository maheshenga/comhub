import { memo } from 'react';

const RunResultPanel = memo(() => {
  return <div data-testid="module-app-run-result" />;
});

RunResultPanel.displayName = 'RunResultPanel';

export default RunResultPanel;
